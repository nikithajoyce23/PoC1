pipeline {
    agent any
    stages {
        stage("Clone Git Repository") {
            steps {
                git(
                    url: "https://github.com/nikithajoyce23/PoC1.git",
                    branch: "master",
                    changelog: true,
                    poll: true
                )
            }
        }
    }
}
